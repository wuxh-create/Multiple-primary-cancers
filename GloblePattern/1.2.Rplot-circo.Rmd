---
title: "MPC circo"
author: "luohh"
date: "2023-12-20"
output: html_document
---



```{r}
### CircoPlot
### 示意图
library(circlize)

set.seed(999)
mat = matrix(sample(18, 18), 6, 6) 
rownames(mat) = paste0("E", 1:6)
colnames(mat) = paste0("E", 1:6)

set.seed(999)
mat = matrix(sample(18, 18), 3, 6) 
rownames(mat) = paste0("S", 1:3)
colnames(mat) = paste0("E", 1:6)

circos.par(gap.after = c(rep(5, nrow(mat)-1), 5))

chordDiagram(mat)

# ---------------------------------------------------------------
origin <- paste0("orig ", sample(c(1:10), 20, replace = T))
destination <- paste0("dest ", sample(c(1:10), 20, replace = T))
data <- data.frame(origin, destination)

adjacencyData <- with(data, table(origin, destination))

circos.clear()

####################################################################################################################################
### 10 x 10

library(circlize)

pcancerData <- read.table("/home/luohh/UKB50wMultiPcancer/05.otherAnalysis/01.globlePattern/03.result/1.MPCpair.table",header = T,stringsAsFactors = F)

pcancerData <- as.matrix(pcancerData)

#circos.initialize()
circos.par(gap.after = c(rep(1, nrow(pcancerData)-1), 1))

chordDiagram(pcancerData, order = c("headAndNeck", "esophagus", "stomach", "smallIntestine", "colorectal", "anus", "liver", "gallbladderAndBiliaryTract","otherDigestive", 
                                    "pancreas","thyroid","otherEndocrine","lung","otherRespiratory","bone","melanoma","mesothelioma","kaposiSarcoma","softTissueSarcoma",
                                    "breast","cervix","uterus","ovary","otherFemaleGenital","prostate","testis","otherMaleGenital","kidney","urinaryBladder",
                                    "otherUrinaryOrgans","eyeAndOrbit","brain","otherNervousSystem","tCellAndNKCellNeoplasms","lymphoidNeoplasms","myeloidNeoplasms"))

chordDiagram(pcancerData)

circos.clear()
```

